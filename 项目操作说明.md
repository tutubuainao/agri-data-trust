# 农业数据可信度检测项目操作说明

更新时间：2026-05-21

这个文件用于记录本项目后续维护、修改、部署时你需要做什么，以及 Codex 会怎么处理。以后流程变化时，需要同步更新这个文件。

## 项目地址

本地项目：

```text
/Users/574xu/Documents/Codex/农业数据真实性检验/agri-data-trust
```

线上项目：

```text
/opt/agri-data-trust
```

线上访问地址：

```text
http://39.106.238.181/agri-trust/
```

服务器服务名：

```text
agri-data-trust.service
```

## 以后修改项目时，你需要怎么做

如果你希望 Codex 帮你改功能、改算法、改页面、改报告格式，直接说明需求即可，例如：

```text
帮我增加一个缺失值异常检测指标，并部署上线
```

或者：

```text
帮我把高风险阈值改成 60 分以下，并更新线上版本
```

Codex 默认会按这个流程处理：

```text
本地改代码 → 本地测试 → 运行 deploy.sh 上传服务器 → 重启线上服务 → 打开线上地址验证
```

你一般不需要登录阿里云控制台，也不需要手动输入服务器命令。

## 如果你自己修改代码

如果你自己在本地改了代码，改完后运行：

```bash
cd /Users/574xu/Documents/Codex/农业数据真实性检验/agri-data-trust
./deploy.sh
```

脚本会自动：

```text
打包本地项目
上传到阿里云服务器
更新 /opt/agri-data-trust
安装或校验 Python 依赖
重启 agri-data-trust.service
验证线上地址
```

## GitHub 同步方案（方案 2）

目标：把本地项目同步到 GitHub，作为代码备份、版本记录和后续协作入口。当前推荐路线是：

```text
本地项目 → GitHub 私有仓库 → 本地 deploy.sh 部署到阿里云服务器
```

也就是说，GitHub 先负责“保存代码和版本”，线上网站仍然由当前的 `deploy.sh` 发布。这样风险最低，后续如果需要，再升级成：

```text
本地提交代码 → 推送 GitHub → GitHub Actions 自动部署到阿里云
```

### 当前已准备好的内容

已经新增 `.gitignore`，用于避免把这些内容上传到 GitHub：

- `.venv/` Python 虚拟环境
- `reports/` 运行时生成的报告和图片
- `__pycache__/` Python 缓存
- `.mplconfig/`、`.cache/` 本地缓存
- `.env`、`*.pem`、`*.key`、SSH 私钥等敏感文件

建议 GitHub 仓库先建成“私有仓库”。如果以后想公开项目，需要先检查 `deploy.sh` 和本文档里的服务器地址、部署细节是否适合公开。

### 第一次同步 GitHub 时，你需要做什么

当前本机的 `git` 命令被 macOS 的 Xcode license 提示拦住了。你需要在本机终端只处理一次：

```bash
sudo xcodebuild -license
```

然后按终端提示阅读并输入同意。这个操作完成后，Codex 才能继续执行 `git init`、`git commit`、`git push`。

你还需要告诉 Codex 两件事：

```text
GitHub 仓库名：例如 agri-data-trust
仓库权限：私有 / 公开
```

推荐：

```text
仓库名：agri-data-trust
仓库权限：私有
```

如果你已经在 Chrome 登录 GitHub，Codex 可以用浏览器帮你创建仓库；如果 GitHub 要求短信、邮箱、二次验证或确认创建，你需要手动点一下或输入验证码。

### 第一次同步时 Codex 会做什么

在你处理完 Xcode license，并确认 GitHub 仓库名后，Codex 会执行：

```bash
cd /Users/574xu/Documents/Codex/农业数据真实性检验/agri-data-trust
git init
git add .
git commit -m "Initial agri data trust MVP"
git branch -M main
git remote add origin <你的 GitHub 仓库地址>
git push -u origin main
```

如果仓库已经存在，Codex 会直接绑定远程仓库并推送；如果还不存在，会先通过 GitHub 页面创建。

### 以后日常修改和同步

以后你可以直接说：

```text
帮我改这个功能，同步 GitHub，并部署上线
```

Codex 默认会按这个流程处理：

```text
本地改代码 → 本地测试 → git commit → git push → ./deploy.sh → 打开线上地址验证
```

如果你自己改了代码，手动同步 GitHub 的基本命令是：

```bash
cd /Users/574xu/Documents/Codex/农业数据真实性检验/agri-data-trust
git status
git add .
git commit -m "说明这次改了什么"
git push
```

然后再部署线上：

```bash
./deploy.sh
```

## 常用命令

本地运行测试：

```bash
cd /Users/574xu/Documents/Codex/农业数据真实性检验/agri-data-trust
source .venv/bin/activate
streamlit run app.py
```

一键部署上线：

```bash
cd /Users/574xu/Documents/Codex/农业数据真实性检验/agri-data-trust
./deploy.sh
```

检查线上服务状态：

```bash
ssh -i ~/.ssh/agri_data_trust_deploy root@39.106.238.181 'systemctl status agri-data-trust --no-pager'
```

重启线上服务：

```bash
ssh -i ~/.ssh/agri_data_trust_deploy root@39.106.238.181 'systemctl restart agri-data-trust'
```

## 什么时候需要你配合

以下情况可能需要你操作：

- 阿里云账号重新登录、短信验证码、人机验证、MFA 验证
- 服务器重装系统或更换 IP
- 本机 `~/.ssh/agri_data_trust_deploy` 被删除
- 阿里云安全组、防火墙、域名解析需要人工确认
- WordPress 后台需要你自己登录确认页面内容
- 涉及账号密码修改、付费、备案、域名购买等操作

除此之外，普通代码修改和上线部署通常不需要你额外操作。

## 不要在聊天里发送的信息

为安全起见，不要在聊天里发送：

- WordPress 密码
- 阿里云账号密码
- 服务器 root 密码
- 数据库密码
- 私钥文件内容
- 短信验证码或 MFA 验证码

如果必须授权访问服务器，优先使用 SSH key 或临时权限。

## 后续维护原则

- 所有结果只输出“可信度评分”和“可疑风险”，不判断数据一定为假。
- 修改算法前，优先保证项目能运行、能上传、能生成报告。
- 每次改完线上功能，都要验证页面能打开，并至少跑一次示例数据。
- 如果部署流程、服务器路径、线上地址、服务名发生变化，需要更新本文件。
