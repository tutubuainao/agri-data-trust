# 农业数据可信度检测项目操作说明

更新时间：2026-05-23

这份文档是本项目的维护手册。目标是：你看完以后，能知道项目文件在哪里、前后端怎么实现、服务器怎么连接、自己修改代码后怎么同步 GitHub 和部署上线。

## 1. 当前网站整体关系

当前线上网站由两个部分组成：

```text
http://39.106.238.181/                 WordPress 主站 / 个人作品集首页
http://39.106.238.181/agri-trust/      农业数据可信度检测系统
http://39.106.238.181/agri-trust/methodology  评分标准与实现原理
```

它们不是同一个程序，但通过同一个服务器和同一个域名入口整合在一起：

```text
WordPress = 主站、作品集、项目入口、简历展示
FastAPI + Vue = 农业数据可信度检测子应用
Nginx = 把 /agri-trust/ 这个路径转发给 Python 服务
GitHub = 保存 Python 项目代码和版本
deploy.sh = 从本地把 Python 项目部署到阿里云服务器
```

所以你本地打开：

```text
http://127.0.0.1:8501/agri-trust/
```

只能看到农业检测系统，看不到 WordPress 主站。这是正常的，因为 WordPress 主站在服务器上运行，本地没有完整 WordPress 环境。

## 2. 本地项目位置

本地项目目录：

```text
/Users/574xu/Documents/Codex/农业数据真实性检验/agri-data-trust
```

进入项目：

```bash
cd /Users/574xu/Documents/Codex/农业数据真实性检验/agri-data-trust
```

## 3. 线上项目位置

阿里云服务器项目目录：

```text
/opt/agri-data-trust
```

系统服务名：

```text
agri-data-trust.service
```

Python 服务监听：

```text
127.0.0.1:8501
```

Nginx 对外路径：

```text
/agri-trust/
```

## 4. 项目目录说明

主要目录和文件：

```text
agri-data-trust/
├── api_app.py                 FastAPI 线上入口
├── app.py                     旧版 Streamlit 入口，保留作参考
├── deploy.sh                  一键部署脚本
├── ops/
│   └── update_wordpress_homepage_enterprise.php
│                                WordPress 首页更新脚本，本地留档版本
├── requirements.txt           Python 依赖
├── README.md                  项目介绍
├── 项目操作说明.md             当前维护手册
├── data/
│   ├── sample_real.csv        模拟真实农业传感器数据
│   └── sample_fake.csv        模拟过度平滑数据
├── src/
│   ├── analysis_service.py    把算法结果封装成 API 返回结构
│   ├── loader.py              CSV / Excel 读取，支持多 sheet 合并和空 sheet 跳过
│   ├── profiler.py            字段识别、文件粗筛、数据概况、少量有效样本列剔除
│   ├── smoothness.py          平滑度检测
│   ├── timeseries.py          时间序列自然性检测
│   ├── digit_analysis.py      小数位和数字规律检测
│   ├── outlier.py             异常点分布检测
│   ├── drift.py               传感器漂移检测
│   ├── correlation.py         多变量相关性检测
│   ├── scoring.py             总分和风险等级
│   └── report.py              HTML 报告生成
├── web/
│   ├── index.html             Vue 前端主页面
│   ├── methodology.html       评分标准与实现原理页面
│   └── assets/
│       ├── app.js             Vue 交互逻辑
│       ├── theme.js           Dark / Light 主题切换
│       └── styles.css         前端样式
└── reports/                   运行时生成的 HTML 报告和图表
```

不要手动上传这些内容到 GitHub：

```text
.venv/
reports/ 里生成的大量报告图片
__pycache__/
.cache/
.mplconfig/
*.pem
*.key
.env
```

这些已经由 `.gitignore` 控制。

## 5. 前后端实现方式

当前 `/agri-trust/` 已经从 Streamlit 升级为：

```text
FastAPI 后端接口 + Vue 前端页面 + src/ 里的统计检测算法
```

后端：

```text
api_app.py
```

负责：

```text
1. 提供 /agri-trust/ 页面
2. 提供 /agri-trust/methodology 页面
3. 接收 CSV / XLSX / XLS 上传
4. 调用 src/analysis_service.py
5. 返回可信度评分、子评分、风险解释、文件粗筛、图表地址、报告地址
```

前端：

```text
web/index.html
web/assets/app.js
web/assets/styles.css
web/assets/theme.js
```

负责：

```text
1. 上传文件
2. 加载真实示例和平滑示例
3. 展示总可信度评分
4. 展示六类指标子评分
5. 展示文件粗筛表
6. 展示上传数据预览表，默认展示系统读取到的前 30 行
7. 展示风险解释和图表
8. 打开 HTML 检测报告
9. 支持 Dark / Light 风格切换
10. 显示上传分析进度和累计分析统计
```

评分标准页面：

```text
web/methodology.html
```

用于解释：

```text
1. 风险等级
2. 总分公式
3. 文件粗筛规则
4. 缺失值处理
5. 六类检测指标原理
6. 六类指标的连续扣分公式：每个风险项从阈值开始逐渐扣分，越接近极端异常扣分越高，不再使用“刚超过阈值就扣固定分”的硬开关。
7. 传感器漂移检测的传统统计实现：前后窗口均值、滚动均值斜率、CUSUM、前后方差变化和变化方向单一性。
```

## 6. WordPress 主站是怎么做的

WordPress 主站地址：

```text
http://39.106.238.181/
```

WordPress 网站目录：

```text
/www/wwwroot/39.106.238.181
```

当前首页页面信息：

```text
页面标题：首页
页面 slug：portfolio-home
页面 ID：59
```

当前根地址已经改成“个人项目作品集”首页，页面里包含：

```text
1. 个人简介入口
2. 农业数据可信度检测项目入口
3. 评分标准与实现原理入口
4. 底部“后续项目框架”里的 PPT放映测试入口
```

首页预览图位置：

```text
/www/wwwroot/39.106.238.181/wp-content/uploads/2026/05/agri-trust-preview.png
```

### WordPress 可以自己建 HTML 当主页面吗

可以，但要理解它的方式。

WordPress 不是直接把一个 `index.html` 文件当首页用，而是把“页面内容”存到数据库里。你可以在后台新建一个页面，把 HTML 结构写进页面正文，然后把它设置为首页。

后台路径通常是：

```text
WordPress 后台 → 页面 → 新建页面 / 编辑首页
WordPress 后台 → 设置 → 阅读 → 主页显示 → 一个静态页面
```

但是要注意：

```text
页面正文：适合放 HTML 结构
页面样式：不要把大段 <style> 直接塞进正文
```

之前页面混乱的原因就是 WordPress 把页面正文里的 `<style>` 样式当成普通文本或被编辑器处理了。以后改首页时建议：

```text
HTML 结构：放在 WordPress 页面正文
CSS 样式：放在 WordPress 后台 → 外观 → 自定义 → 额外 CSS
```

当前 Codex 更新 WordPress 首页使用的是服务器脚本：

```text
/root/codex-wp-tasks/update_wordpress_homepage_enterprise.php
```

本地仓库也保留了一份同名脚本，便于版本管理：

```text
ops/update_wordpress_homepage_enterprise.php
```

这个脚本会：

```text
1. 更新 WordPress 的 portfolio-home 页面正文
2. 把首页样式写入 WordPress 的 wp-custom-css
3. 避免把大段 CSS 直接塞进页面正文导致显示混乱
4. 把“在线检测系统”和“评分标准”链接设置为新标签页打开
5. 保留右上角“个人简介”入口，主页主体专注展示项目
6. 在底部项目框架中展示 PPT放映测试等普通 WordPress 页面入口
```

Codex 本机临时脚本副本曾经放在：

```text
/private/tmp/update_wordpress_homepage_enterprise.php
```

`/private/tmp` 只是临时工作目录，不是长期保存位置。长期记录以服务器脚本和本文档为准。

## 7. 服务器远程连接

现在你的电脑已经配置了 SSH 快捷方式，可以直接：

```bash
ssh aliyun
```

进入服务器后，看到类似：

```text
root@iZxxxxxx:~#
```

说明已经登录成功。

SSH 快捷配置通常在：

```text
~/.ssh/config
```

当前逻辑是：

```text
Host aliyun
    HostName 39.106.238.181
    User root
    IdentityFile ~/.ssh/agri_data_trust_deploy
```

部署脚本默认使用这把服务器私钥：

```text
~/.ssh/agri_data_trust_deploy
```

GitHub 推送使用的是另一把 key：

```text
~/.ssh/agri_data_trust_github
```

私钥不要发到聊天里，不要上传 GitHub。

## 8. 本地开发和测试

进入项目：

```bash
cd /Users/574xu/Documents/Codex/农业数据真实性检验/agri-data-trust
```

启动本地服务：

```bash
source .venv/bin/activate
uvicorn api_app:app --reload --host 127.0.0.1 --port 8501
```

浏览器打开：

```text
http://127.0.0.1:8501/agri-trust/
```

评分标准页面：

```text
http://127.0.0.1:8501/agri-trust/methodology
```

停止本地服务：

```text
在终端按 Ctrl + C
```

快速检查 Python 代码是否能编译：

```bash
python -m compileall api_app.py src
```

## 9. 自己修改后的标准流程

如果你自己改了代码，推荐完整流程是：

```bash
cd /Users/574xu/Documents/Codex/农业数据真实性检验/agri-data-trust

git status
python -m compileall api_app.py src

git add .
git commit -m "说明这次改了什么"

git push
./deploy.sh
```

含义：

```text
git status      看哪些文件被修改
compileall      检查 Python 代码有没有语法错误
git add         把改动加入本次提交
git commit      在本地保存一个版本
git push        同步到 GitHub
./deploy.sh     上传到阿里云服务器并重启线上服务
```

如果你想先上线验证，再同步 GitHub，也可以：

```bash
./deploy.sh
git push
```

但更推荐：

```text
先 commit / push，再 deploy
```

这样线上每次发布都有对应的代码版本，后面更容易回退和排查。

## 10. GitHub 同步

当前 GitHub 仓库：

```text
https://github.com/tutubuainao/agri-data-trust
```

仓库权限：

```text
Private
```

当前远程仓库应显示：

```text
git@github.com:tutubuainao/agri-data-trust.git
```

查看命令：

```bash
git remote -v
```

普通同步命令：

```bash
git status
git add .
git commit -m "说明这次改了什么"
git push
```

这把 GitHub Deploy key 的作用：

```text
~/.ssh/agri_data_trust_github
```

```text
1. 让本机可以直接 git push 到这个仓库
2. 权限只限这个仓库
3. 不需要保存 GitHub 密码
4. 普通代码同步不需要打开 Chrome 登录 GitHub
```

以下情况仍然需要你打开 GitHub 或 Chrome：

```text
1. 新建另一个 GitHub 仓库
2. 修改仓库权限、Deploy key、协作者
3. GitHub 要求验证码或二次验证
4. 公开仓库、删除仓库、转移仓库
```

## 11. 部署到线上

一键部署：

```bash
cd /Users/574xu/Documents/Codex/农业数据真实性检验/agri-data-trust
./deploy.sh
```

`deploy.sh` 会自动：

```text
1. 打包本地项目
2. 排除 .venv、reports、大量缓存文件
3. 上传压缩包到阿里云服务器
4. 解压到 /opt/agri-data-trust
5. 复用或创建服务器 Python 虚拟环境
6. 安装 requirements.txt 依赖
7. 写入 /etc/systemd/system/agri-data-trust.service
8. 重启 agri-data-trust 服务
9. 检查 Nginx 配置
10. 验证 /agri-trust/ 和 /agri-trust/methodology 能访问
```

部署脚本还会配置一个维护页：

```text
/www/wwwroot/39.106.238.181/agri-trust-updating.html
```

当系统正在重启或发布时，Nginx 会优先显示“系统正在更新”的提示页。这样用户不会误以为网站已经坏掉。维护状态由这个临时文件控制：

```text
/tmp/agri_data_trust_maintenance
```

正常部署结束后，脚本会自动删除这个临时文件。

当前上传处理能力：

```text
1. CSV：按单表读取。
2. XLSX / XLS：遍历所有 sheet，空 sheet 自动跳过。
3. 多 sheet：会把可用 sheet 按“纵向追加行”的方式合并分析，并在页面展示已合并和已跳过的 sheet。
   - 系统会自动添加 `__sheet__` 来源列，记录每一行来自哪个 sheet。
   - 合并时按列名对齐，同名列进入同一列，不同 sheet 独有的列在其他 sheet 行中记为空值。
   - `__sheet__` 只用于溯源展示，在文件粗筛中标记为“Sheet 来源列”，不参与可信度评分。
4. 两行表头：会尝试合并上层表头和下层表头，例如“性诱集数量（头/台）-诱捕器1”。
5. 少量有效值列：如果某列只有少量数值，而其他行大多为空，会剔除并在文件粗筛中说明。
6. 编号/代码列：序号、编号、行政区代码等标识列不进入可信度评分。
7. 上传统计：每次真实上传会记录已分析文件数、累计行数、累计 sheet 数；示例数据不计入真实上传统计。
8. 首次打开检测页面不再默认分析 sample_real.csv；用户需要主动上传文件或点击示例按钮。
9. 同一浏览器会话内会暂存上一次分析结果，用户从其他页面返回检测系统时会恢复刚才的文件结果。
10. 如果未识别到可分析数值列，页面会弹窗提示，并在可信度评分区域显示“无法评分 / 未识别”。
11. 图表使用中文字体自动探测；中文列名会尽量在趋势图、尾数图和相关性热力图中正常显示。
12. 图表卡片下方展示用途说明，不使用鼠标悬停弹窗。
13. 页面会在文件粗筛后展示上传数据预览，方便用户确认系统读取的列、行和 sheet 合并结果。
```

统计数据使用 SQLite 数据库单独保存，不放在项目发布目录里。这样代码更新、重新部署、替换 `/opt/agri-data-trust` 时，累计分析次数不会清零。

```text
/opt/agri-data-trust-data/usage.sqlite
```

当前记录内容包括：已分析文件数、累计数据行数、累计数值列数、累计 sheet 数、最后一次分析文件名。旧版 `reports/usage_stats.json` 如果存在，会在首次创建 SQLite 时自动迁移。

部署成功后会看到类似：

```text
Server deployment OK
Deploy complete: http://39.106.238.181/agri-trust/
```

线上验证地址：

```text
http://39.106.238.181/
http://39.106.238.181/agri-trust/
http://39.106.238.181/agri-trust/methodology
```

## 12. 服务器常用命令

登录服务器：

```bash
ssh aliyun
```

查看服务状态：

```bash
systemctl status agri-data-trust --no-pager
```

重启服务：

```bash
systemctl restart agri-data-trust
```

查看服务日志：

```bash
journalctl -u agri-data-trust -n 100 --no-pager
```

查看 Nginx 配置是否正确：

```bash
nginx -t
```

重载 Nginx：

```bash
nginx -s reload
```

退出服务器：

```bash
exit
```

如果你不想先 `ssh aliyun` 进入服务器，也可以在本地直接执行：

```bash
ssh aliyun 'systemctl status agri-data-trust --no-pager'
ssh aliyun 'systemctl restart agri-data-trust'
ssh aliyun 'journalctl -u agri-data-trust -n 100 --no-pager'
```

## 13. WordPress 主站后续怎么改

小改文字、链接：

```text
WordPress 后台 → 页面 → 首页
```

改样式：

```text
WordPress 后台 → 外观 → 自定义 → 额外 CSS
```

大改首页结构：

```text
让 Codex 修改本地脚本并同步到服务器：
ops/update_wordpress_homepage_enterprise.php
/root/codex-wp-tasks/update_wordpress_homepage_enterprise.php
```

大改后需要验证：

```text
http://39.106.238.181/
http://39.106.238.181/agri-trust/
```

不建议把农业检测系统直接塞进 WordPress 页面里，例如用 iframe 嵌入。原因：

```text
1. 上传文件体验容易受影响
2. 报告下载和静态资源路径更容易出问题
3. 移动端适配会更麻烦
4. 子应用独立部署更清晰
```

当前推荐路线是：

```text
WordPress 主站负责展示和入口
FastAPI + Vue 子应用负责真实交互功能
```

如果要把某个 WordPress 页面显示到主页，例如“PPT放映测试”，推荐放在主页底部“后续项目框架”区域，做成项目卡片链接。当前 `PPT放映测试` 页面 ID 是 17，脚本里通过 `get_permalink(17)` 自动获取链接。

## 14. Codex 帮你修改时的默认流程

你可以直接说：

```text
帮我改这个功能，同步 GitHub，并部署上线
```

Codex 默认会按：

```text
本地改代码 → 本地测试 → git add → git commit → git push → ./deploy.sh → 打开线上地址验证
```

如果你只说：

```text
同步 GitHub
```

那就只会提交并推送到 GitHub，不会自动部署服务器。

如果你说：

```text
部署上线
```

那就会运行 `./deploy.sh` 发布到阿里云。

## 15. 安全注意

不要在聊天里发送：

```text
1. WordPress 密码
2. 阿里云账号密码
3. 服务器 root 密码
4. 数据库密码
5. SSH 私钥内容
6. 短信验证码或 MFA 验证码
```

如果必须授权访问服务器，优先使用：

```text
SSH key
临时权限
浏览器中你自己确认
```

## 16. 后续维护原则

项目原则：

```text
1. 只输出“可信度评分”和“可疑风险”
2. 不判断“这个数据一定是假的”
3. 所有结果都要可解释
4. 第一版优先保证能运行、能上传、能生成报告
5. 修改算法时要保留文件粗筛和评分原理说明
6. 每次部署后至少验证页面能打开，并跑一次示例数据
```

如果这些内容发生变化，需要同步更新本文档：

```text
1. 服务器 IP
2. 线上目录
3. 服务名
4. GitHub 仓库地址
5. WordPress 首页页面 ID 或 slug
6. Nginx 路径
7. 部署脚本逻辑
8. 本地开发命令
```
